[tool.poetry]
name = "pokeai"
version = "0.1.0"
description = "Pokémon Showdown RL agent"
authors = ["Your Name <you@email.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
poke-env = ">=0.8"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"
jupyter = "^1.0"
ipykernel = "^6.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
